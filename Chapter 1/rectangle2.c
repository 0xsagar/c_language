#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter tha length of rectangle: ");
    scanf("%d", &a);
    printf("Enter the breadth of rectangle: ");
    scanf("%d", &b);
    printf("The area of the rectangle is %d", a * b);
    return 0;
}